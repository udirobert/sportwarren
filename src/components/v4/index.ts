/**
 * V4 Stadium — barrel export.
 *
 *   import { V4StadiumBackdrop, V4Heading, PALETTE } from '@/components/v4';
 *
 * V4 is the dark stadium / atmospheric register used on marketing
 * surfaces (homepage, landing sections). V3 (`@/components/v3`) is
 * the editorial cream / archival register used on player cards +
 * in-app surfaces. Both share Antonio + JetBrains Mono typography.
 *
 * The homepage currently inlines V4 as Tailwind utilities — these
 * primitives are the same pattern extracted into reusable
 * components, available for future V4 surfaces.
 */

export * from './tokens';
export * from './primitives';
